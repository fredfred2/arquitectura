Running the JPAEmployeeExample:
1) Create the EmployeeDB database
    > Right-click Java DB in the Services tab and select Create Database
    > Enter EmployeeDB as the database name and oracle as the user and password
    > Click OK
2) Populate the database
    > Open EmployeeDB.sql in the editor
    > Select jdbc:derby://localhost:1527/EmployeeDB as the Connection
    > Click the Run SQL button (to the right of the connection)
3) Run the application
